namespace BootcampProject.Entities.Enums
{
    public enum BootcampState
    {
        Preparing,
        OpenForApplication,
        InProgress,
        Finished,
        Cancelled
    }
}
